export component MainWindow {
    width: 600px;
    height: 400px;

    Text {
        text: "Hello, Slint!";
        color: blue;
        font-size: 24px;
        horizontal-alignment: center;
        vertical-alignment: center;
    }
}
